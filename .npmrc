# NPM Workspace Settings
# (npm uses workspaces by default when configured in package.json)

# Save exact versions (not ranges) for better reproducibility
save-exact=true

# Engine strict - enforce Node version from package.json
engine-strict=true

# Lockfile settings
package-lock=true

# Legacy peer dependencies (suppress warnings)
legacy-peer-deps=false
